# 项目文档整理与重写清单

状态：第一阶段盘点稿；第五步归档已完成

本文记录文档的当前地位、归档动作和后续维护规则。后续整理必须以当前架构主线为准：

> FP64 C++ 黄金参考 + FP32/FP64 SystemC 硬件架构模型 + 局部 INT32 GEMM SystemC 行为模型；软件使用命令流/描述符作为目标交互物，现有 ABI v2 作为迁移阶段的当前运行接口。

## 1. 文档状态定义

| 状态 | 含义 | 后续动作 |
|---|---|---|
| `CANONICAL` | 当前架构唯一依据 | 保留，必要时做一致性修订 |
| `REWRITE` | 仍属于当前主线，但内容已落后或职责不清 | 原路径重写，不直接归档 |
| `ARCHIVE-HISTORY` | 有实验、决策或正式提交价值，但不再指导实现 | 移至 `archive/`，保留原始内容 |
| `ARCHIVE-UNUSED` | 已被否定、与当前方向冲突或只是对话提示词 | 移至 `unused/`，不再作为参考依据 |
| `KEEP-REFERENCE` | 参考论文、正式材料、专利材料等 | 保留原位置，不纳入当前架构文档 |

“过时”不等于“没有价值”。实验结果、开题稿和正式提交材料应归档保存；只有被否定的设计草案、旧提示词和重复副本才适合放入 `unused/`。

## 2. 当前主线文档

| 文件 | 状态 | 判断与动作 |
|---|---|---|
| [`项目总体架构重新定型方案.md`](项目总体架构重新定型方案.md) | `CANONICAL` | 当前唯一架构主线。保留在根目录；待 M0 冻结后再改为正式版本，不与旧方案并列竞争。 |
| [`README.md`](README.md) | `REWRITE` | 仍然是仓库入口，但需要改成“项目范围、三种运行后端、当前代码入口、验证命令、文档索引”。不能继续把旧 ABI、QAU 和固定 BFP 写成最终方向。 |
| [`todo.md`](todo.md) | `REWRITE` | 当前任务表仍围绕 ABI v2、全局定点/BFP 和 QAU RTL 展开，需要重写为六个月计划：SystemC 浮点硬件模型、命令流迁移、INT32 GEMM 行为模型、控制 RTL 和验证。 |
| [`archive/history/root/problem.md`](archive/history/root/problem.md) | `ARCHIVE-HISTORY` | 记录的结构非对称输入问题已经修复，适合作为修复记录保留，不应继续作为“待统一问题”驱动开发。 |

## 3. `graduation-code/` 主线说明文档

| 文件 | 状态 | 判断与动作 |
|---|---|---|
| [`graduation-code/README.md`](graduation-code/README.md) | `REWRITE` | 内容接近当前实现，但仍把 QAU、全局定点稳定性和旧模块边界当作主线。重写为代码目录入口，并明确 SystemC 是完整系统模型，RTL 是控制/地址/关键原型。 |
| [`archive/history/code/README_code.md`](archive/history/code/README_code.md) | `ARCHIVE-UNUSED` | 明确是早期架构设计，包含当前不存在或不作为项目目标的 `Matrix_Engine`、SFU、Tensor Core、RV64 固件和 AXI-Lite 体系。不得继续作为代码说明。 |
| [`archive/history/code/进度.md`](archive/history/code/进度.md) | `ARCHIVE-HISTORY` | 早期模块进度记录，不能反映当前实现和六个月计划。保留历史，不作为当前 todo。 |
| [`graduation-code/software/README.md`](graduation-code/software/README.md) | `REWRITE` | 软件实现本身仍然有效，但 README 需要区分“当前 ABI v2 产物”和“目标 command compiler”，不能暗示命令流已经实现。 |
| [`graduation-code/systemc/README.md`](graduation-code/systemc/README.md) | `REWRITE` | 优先级最高。当前文档把 int32+BFP 当作主要后端，且没有清楚区分 C++ 数值参考、SystemC 硬件模型和未来 RTL 替换件。需要重写为三种后端：`FP64_GOLDEN`、`FP32_SYSTEMC`、`FP32_SYSTEMC_INT32_GEMM`。 |
| [`graduation-code/systemc/docs/ABI_v2.md`](graduation-code/systemc/docs/ABI_v2.md) | `REWRITE` | ABI v2 仍被当前代码使用，不能归档；但它应被重新定位为“迁移阶段的当前产物 ABI”，并增加目标 command stream/descriptor 接口的迁移说明。 |
| [`archive/precision-history/FIXED_POINT_STABILITY_DESIGN.md`](archive/precision-history/FIXED_POINT_STABILITY_DESIGN.md) | `ARCHIVE-HISTORY` | 这是当前旧定点/BFP 实现的真实实验记录，具有复现价值，但全局定点路径已不再是新的架构主线。保留为历史实验，不重写成新架构说明。 |
| [`graduation-code/hardware/README.md`](graduation-code/hardware/README.md) | `REWRITE` | 需要重新定义 RTL 范围：GCU/ATU/HPU/存储接口为主要 RTL，INT32 GEMM 暂无 RTL 时由 SystemC 行为模型承担，Panel-LU/TRSM 首先在 SystemC 中建模。 |
| [`graduation-code/hardware/HPU/HPU功能确定.md`](graduation-code/hardware/HPU/HPU功能确定.md) | `REWRITE` | 文件很短且明确写有“这点存疑”。应重写为确定性的 HPU 规范，先固定最大绝对值归约、tie-break、valid/ready、pivot failure 和候选边界，暂不把 CALU/阈值双模式作为首版必需功能。 |
| [`graduation-code/hardware/QAU/README.md`](graduation-code/hardware/QAU/README.md) | `REWRITE` | 当前 QAU 是全局 front 装配和 BFP 再量化单元，与新方案的“量化只在 GEMM 调用边界发生”不一致。应重写为 `Precision Adapter / Quantize-Dequantize` 的候选职责，是否保留 QAU 名称在 M0 决定。 |
| [`graduation-code/sim/README.md`](graduation-code/sim/README.md) | `REWRITE` | 目前几乎只有运行命令，需说明它是历史 Python 行为模型/实验工具，不是 SystemC 或 RTL 的替代品。 |
| [`graduation-code/sim/results/hardware_behavior_latest/report.md`](graduation-code/sim/results/hardware_behavior_latest/report.md) | `ARCHIVE-HISTORY` | 2026-06-15 的旧定点行为实验报告。数值结果仍可作为历史对照，但不能作为新 FP32 SystemC/INT32 GEMM 方案的主结果。 |

## 4. `optimization-v1/v2/v3` 独立副本

以下三个目录是独立实验分支，不应再和主线并列展示：

| 目录 | 状态 | 判断 |
|---|---|---|
| `graduation-code/optimization-v1/` | `ARCHIVE-UNUSED` | 行列均衡 A/B 实验副本。结论可保留，但代码和文档不是当前执行路径。 |
| `graduation-code/optimization-v2-tile-bfp/` | `ARCHIVE-UNUSED` | 16×16 Tile BFP 方案，曾经是稳定性优化方向，现已被“浮点 SystemC + 局部 INT32 GEMM”替代。 |
| `graduation-code/optimization-v3-adaptive-precision/` | `ARCHIVE-HISTORY` | 最新的 QF26/自适应精度实验，有较高实验记录价值，但仍是旧全局 BFP 路线，不能作为新架构依据。 |

处理原则：不删除这些目录中的代码和结果；后续只将其文档移到对应的 `archive/optimization-v1`、`archive/optimization-v2`、`archive/optimization-v3`，或者整体标注为历史实验目录。主线 README 只链接实验结论，不链接这些目录的运行说明。

需要归档的文档族包括：

- 各版本 `README.md`、软件 README、SystemC README；
- `OPTIMIZATION_REPORT.md`、`OPTIMIZED_ABI_EXTENSION.md`；
- `TILE_BFP_ABI.md`、`TILE_BFP_IMPLEMENTATION.md`；
- `ADAPTIVE_PRECISION_OPTIMIZATION.md`；
- 各版本重复的 `ABI_v2.md`、`FIXED_POINT_STABILITY_DESIGN.md` 和结果 README。

## 5. `graduation-project/` 理论、设计和论文文档

### 5.1 需要重写的当前文档

| 文件 | 状态 | 判断与动作 |
|---|---|---|
| [`graduation-project/README_pro.md`](graduation-project/README_pro.md) | `REWRITE` | 当前只是早期目录说明和设计演进摘要。重写为论文资料目录、当前架构主线、历史材料、参考文献和正式提交物的导航。 |
| [`graduation-project/节点计算过程.md`](graduation-project/节点计算过程.md) | `REWRITE` | 当前描述“TPU 计算矩阵更新”的早期 block-LU 思路。重写为 front 内 `Panel-LU → TRSM → GEMM/Schur` 的数学流程，并明确 SystemC 浮点模型与局部 INT32 GEMM 模型的边界。 |
| [`graduation-project/论文.md`](graduation-project/论文.md) | `REWRITE` | 论文提纲仍包含早期“张量计算驱动”和旧硬件假设。应根据新架构重新编排问题定义、SystemC 模型层次、RTL 范围、精度策略和实验结论。 |
| [`毕业论文.docx`](毕业论文.docx) | `REWRITE` | 作为论文主稿保留，不归档；待架构 M0 冻结后重写与实现边界冲突的章节。 |
| `毕业论文.docx` 配套图片与章节材料 | `REWRITE` | 只保留能服务新论文的图；旧 TPU/纯整数数据流图需要重新绘制。 |

### 5.2 归档的早期设计和讨论

| 文件/目录 | 状态 | 原因 |
|---|---|---|
| [`archive/history/design-notes/FSCHOL调度方法.md`](archive/history/design-notes/FSCHOL调度方法.md) | `ARCHIVE-HISTORY` | 来源是 Cholesky/FSCHOL，能作为调度参考，但不是当前非对称稀疏 LU 主线。 |
| `graduation-project/src/压缩部分.md` | `ARCHIVE-UNUSED` | 空/未形成可执行设计的早期笔记。 |
| `graduation-project/src/小算法部分.md` | `ARCHIVE-HISTORY` | 早期定点 TRSM/LU 推导，保留作为算法草稿，不作为当前接口说明。 |
| `graduation-project/src/量化方案.md` | `ARCHIVE-UNUSED` | 早期全局量化方案，已被后续版本替代。 |
| `graduation-project/src/量化方案V2.md` | `ARCHIVE-HISTORY` | 受控动态范围/装配量化方案，具有历史决策价值，但与当前局部 GEMM 量化边界冲突。 |

### 5.3 开题目录

开题材料是正式过程记录，建议整体放入 `archive/formal/opening/`，不要混入当前架构目录：

| 文件 | 状态 | 说明 |
|---|---|---|
| `开题报告_矩阵求解器_背景与调研.md` | `ARCHIVE-HISTORY` | 正式开题背景和文献调研，保留提交历史。 |
| `毕设20251210版.md` | `ARCHIVE-HISTORY` | 版本化的早期架构总结。 |
| `架构级分析报告：面向稀疏稠密统一求解器的软硬协同设计.md` | `ARCHIVE-HISTORY` | 早期统一求解器分析，部分结论已被新架构替代。 |
| `矩阵求解器_软硬协同方案.md` | `ARCHIVE-HISTORY` | 早期混合精度/IR/硬件方案，不再是主线。 |
| `设备接口规范_工作队列与前沿组装.md` | `ARCHIVE-UNUSED` | 旧的工作队列、FP16/BF16 和命令设计，必须由新的 command stream 文档替代。 |
| `Project_Context_Card_Matrix_Solver_Integer.md` | `ARCHIVE-UNUSED` | 明确要求纯整数、无浮点/IR，与当前 FP32 SystemC 主路径冲突。 |
| `1110回答.md` | `ARCHIVE-UNUSED` | 纯整数专用版架构和旧接口讨论。 |
| `prompt.md` | `ARCHIVE-UNUSED` | 旧对话提示词，包含“不要更改这些设定”等已经失效的约束。 |
| `图片.md` | `ARCHIVE-UNUSED` | 旧 Mermaid 架构图，需根据新架构重新绘制。 |
| `问题总结.md`、`问题总结1107.md`、`问题总结1110.md` | `ARCHIVE-HISTORY` | 讨论过程记录，保留上下文，不作为当前设计依据。 |
| `IR_参考伪代码.md` | `KEEP-REFERENCE` | 可保留为混合精度求解参考，但不属于当前硬件主路径。 |

### 5.4 参考资料和正式材料

| 文件/目录 | 状态 | 处理原则 |
|---|---|---|
| `graduation-project/参考文献/` | `KEEP-REFERENCE` | 不移入 `unused`。论文、奠基文章和文献总结是理论参考库。 |
| `graduation-project/参考文献/文献总结.md` | `KEEP-REFERENCE` | 可补充“对当前架构的实际引用位置”，但不应与架构决策文档混合。 |
| `graduation-project/汇报/` | `KEEP-REFERENCE` | PPT、PDF 和会议材料是正式过程记录，保留。 |
| `毕业论文.docx`、`论文编排.docx` | `REWRITE` / `KEEP-REFERENCE` | 主稿重写；排版模板保留。 |
| `专利/` | `KEEP-REFERENCE` | 专利是独立工作流，不随本次软件/硬件架构文档归档。后续只需单独标注哪些内容仍与新架构一致。 |

## 6. 当前必须新建的文档

在移动旧文档之前，建议先新建以下五份主线文档：

1. `docs/architecture/项目范围与交付边界.md`
   - 从总体架构方案提取最终范围、支持域和明确不做事项。
2. `docs/architecture/系统架构与模块职责.md`
   - 软件、GCU、buffer、ATU、HPU、Panel-LU、TRSM、GEMM、solve 的模块关系。
3. `docs/interface/Command_Stream_and_Descriptor.md`
   - 目标 command stream、descriptor、completion、错误和 NodeTask 迁移关系。
4. `docs/systemc/SystemC_模型规范.md`
   - FP64 golden、FP32 SystemC、INT32 GEMM 行为模型和硬件旁路禁止规则。
5. `docs/verification/验证计划与结果口径.md`
   - 功能正确性、数值误差、周期、DDR bytes、RTL/行为模型边界和论文报告口径。

这些文档完成后，根目录 README、各模块 README 和论文提纲只引用它们，不再重复定义架构。

## 7. 第一阶段结论

### 保留并作为当前依据

- `项目总体架构重新定型方案.md`；
- 当前主线代码和测试；
- 当前 ABI v2 文档，但要标记为迁移阶段接口；
- 参考文献、正式开题/中期/汇报材料、专利材料。

### 必须重写

- 根 README、`todo.md`；
- `graduation-code/README.md`；
- software/SystemC/hardware/sim README；
- ABI v2 迁移说明；
- HPU 规范、QAU/Precision Adapter 规范；
- `graduation-project/README_pro.md`、`节点计算过程.md`、`论文.md` 和论文主稿。

### 应归档

- `README_code.md`、早期 `进度.md`、已解决的 `problem.md`；
- `optimization-v1/v2/v3` 的文档和结果说明；
- 全局 INT32/BFP 量化方案及其开题版本；
- 旧工作队列/FP16/BF16/纯整数 prompt 和旧架构图；
- FSCHOL/Cholesky 方向的设计草稿。

## 8. 整理顺序

1. 先确认本文分类，不移动文件；
2. 新建五份当前主线文档；
3. 重写根 README 和 `todo.md`；
4. 重写代码目录 README 与当前 ABI 迁移说明；
5. 将旧技术方案和实验分支移入 `archive/` 或 `unused/`；
6. 最后重写论文提纲和论文正文，避免论文继续引用被归档方案。

第五步之前没有删除文件；本次只移动已确认的文档，不修改源代码和当前测试入口。

## 9. 第五步执行结果

第五步已完成：

- 已创建 `archive/` 和 `unused/` 及其索引说明；
- 已将历史代码说明、旧问题记录、旧量化设计和旧开题方案移动到归档目录；
- 已将 `optimization-v1/v2` 文档移动到 `unused/`；
- 已将 `optimization-v3` 文档和实验说明移动到 `archive/optimization-v3/`；
- 未移动代码、测试、参考论文、正式汇报、专利材料、论文主稿和 `IR_参考伪代码.md`；
- 未删除任何文件。

移动后的主线入口只有根目录 `README.md`、`todo.md` 和 `docs/` 下的五份规范文档。
