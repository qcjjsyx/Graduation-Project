```python
### 前向代换， Lx = b 
for i in range(32):
    x[i] = b[i]/l[i][i]
    for j in range(i+1,32):
        b[j] -= x[i]*l[j][i]
  

### 量化后的前向代换 LX=A 其中L，A均为 16*16矩阵，且L，A的指数不相同。
### A的指数为e_a， L的指数e_l
for col in range(16): ### 循环A中的每一列
    for i in range(16):
        X[i][col] = (A[i][col]/L[i][i]) ### 确定指数为e_a -e_l
        for j in range(i+1,16):
            A[i][col] -= X[i][col]*L[i][i] ### 乘法指数会恢复为e_a
  
  
  
  
### 后向代换  xU = b 
for i in range(32):
    x[i] = b[i]/u[i][i]
    for j in range(i+1,32):
        b[j] -= x[i]*u[i][j]

### 量化后的 后向代换 XU=A, 其中的U，A均为16*16矩阵，且对应指数不同。
### A的指数为e_a， U的指数e_u
for row in range(16):
    for i in range(16):
        X[row][i] = A[row][i]/U[i][i]
        for j in range(i+1,16):
            A[row][j] -= X[row][i]*U[i][j]
        
        
  
  
  
  
### LU标量分解 尺寸是b*b
for i in range(1,b+1):
    l[i][i] = 1
    u[i][i] = a[i][i]
    for j in range(i+1,b+1):
        u[i][j] = a[i][j]
        l[j][i] = a[j][i]/u[i][i]
    for k in range(i+1,b+1):
        for j in range(i+1,b+1):
            a[k][j] -= l[k][i]*u[j][i]



  




```

需要将上述算法，编写为rv64指令，且需要多次循环
