```python
### 前向代换， Lx = b

for i in range(1,33):
    for j in range(1，i)：
    	b[i] -= l[i][j]*x[j]
    x[i] /= l[i][i]
    
    
    
### 后向代换  xU = b
for i in range(32,0,-1):
    for j in range(32,i,-1):
        b[i] -= u[i][j]x[j]
    x[i] /= b[i]/u[i][i]
    
    
    
    
    
### LU标量分解 尺寸是b*b
for i in range(1,b+1):
    l[i][i] = 1
    u[i][i] = a[i][i]
    for j in range(i+1,b+1):
        u[i][j] = a[i][j]
        l[j][i] = a[j][i]/u[i][i]
    for k in range(i+1,b+1):
        for j in range(i+1,b+1):
            a[k][j] -= l[k][i]*u[j][i]
            


```

需要将上述算法，编写为rv64指令，且需要多次循环

